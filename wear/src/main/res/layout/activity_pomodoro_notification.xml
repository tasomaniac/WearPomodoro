<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:id="@+id/content"
  tools:context=".PomodoroNotificationActivity"
  tools:deviceIds="wear">

  <TextView
    android:id="@+id/pomodoro_time"
    tools:text="23:12"
    android:singleLine="true"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_alignParentStart="true"
    android:layout_toStartOf="@+id/pomodoro_start_stop_button"
    style="@style/TextView.Large.Light"
    android:textColor="@color/notification_primary" />

  <TextView
    android:layout_marginTop="2dp"
    android:id="@+id/pomodoro_description"
    tools:text="Pomodoro #3"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@id/pomodoro_time"
    android:layout_alignParentStart="true"
    android:layout_alignStart="@id/pomodoro_time"
    android:layout_toStartOf="@+id/pomodoro_start_stop_button"
    style="@style/TextView.Medium.Light"
    android:textColor="@color/notification_secondary" />

  <ImageView
    android:id="@+id/pomodoro_start_stop_button"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    tools:src="@drawable/ic_action_stop_grey"
    android:paddingStart="8dp"
    android:layout_alignTop="@id/pomodoro_time"
    android:layout_alignBottom="@id/pomodoro_description"
    android:layout_alignParentEnd="true"
    android:visibility="gone"
    android:contentDescription="@string/cd_start_stop_button" />
</RelativeLayout>

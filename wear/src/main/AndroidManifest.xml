<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.vngrs.android.pomodoro.wear">

  <uses-feature android:name="android.hardware.type.watch" />

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:name=".App"
    android:theme="@android:style/Theme.DeviceDefault">

    <activity
      android:name=".PomodoroWearBroadcastActivity"
      android:label="@string/start_working">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <receiver
      android:name=".PomodoroNotificationReceiver"
      android:exported="true">
      <intent-filter>
        <action android:name="com.vngrs.android.pomodoro.SHOW_NOTIFICATION" />
      </intent-filter>
    </receiver>
  </application>

</manifest>
